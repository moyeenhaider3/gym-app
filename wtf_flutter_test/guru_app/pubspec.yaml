name: guru_app
description: "Guru App — Member (DK) — WTF Flutter AI-Native"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared:
    path: ../shared
  hmssdk_flutter: ^1.10.7
  permission_handler: ^11.3.0
  flutter_bloc: ^8.1.5
  hive_flutter: ^1.1.0
  equatable: ^2.0.5
  http: ^1.2.1
  intl: ^0.19.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
